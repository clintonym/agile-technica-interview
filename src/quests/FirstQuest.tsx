import * as React from "react"
import {Box} from "@chakra-ui/react";

export const FirstQuest = () => {
  return (
    <Box w="full" bg="blue.300">
      {/*TODO: First quests -- Loop n > 1 times to render n > 1 components*/}
      {/*Code here*/}
    </Box>
  );
};
